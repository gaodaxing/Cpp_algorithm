// ConsoleApplication26.cpp : Defines the entry point for the console application.
//

#include "stdafx.h"
#include<iostream>
#include<cstring>
#include<queue>
using namespace std;
struct piece {
	int x;
	int step;
	piece(int m):x(m){}
};
int visited[10000];
void flip(int x, int m, int n) {
	if()
}
int main()
{
	char rec[4][4];
	int board = 0;
	for (int i = 0; i < 4; i++) {
		for (int j = 0; j < 4; j++) {
			cin >> rec[i][j];
			if (rec[i][j] == 'b') board=board|0x1<<i*4+j;
		}
	}
	queue<piece> q;
	q.push(piece(board));
	while (!q.empty()) {
		piece a = q.front();
		if(a.x == 0 || a.x == 0x1 << 15)break;
		else {

		}
	}
    return 0;
}

